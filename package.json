{
  "name": "bot-hello-world",
  "version": "0.1.0",
  "description": "Hello world project to learn bot framework",
  "repository": {
    "type": "git",
    "url": "https://github.com/beth-panx/bot"
  },
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "tsc": "tsc -p tsconfig.json",
    "tsc-watch": "tsc -p tsconfig.json -w",
    "build": "rimraf app && concurrently \"npm run tsc\"",
    "start": "rimraf app && concurrently \"npm run tsc-watch\""
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "botbuilder": "^3.9.0",
    "restify": "^5.0.1"
  },
  "devDependencies": {
    "@types/node": "^8.0.19",
    "@types/restify": "^5.0.1",
    "concurrently": "^3.5.0",
    "rimraf": "^2.6.1",
    "typescript": "^2.4.2"
  }
}
